diff --git a/core/include/server_main.h b/core/include/server_main.h
index 5d0b0c6..9ee4391 100644
--- a/core/include/server_main.h
+++ b/core/include/server_main.h
@@ -3,17 +3,20 @@
 
 #include "QString"
 #include <QObject>
+#include <atomic>
 
 class Server : public QObject {
     Q_OBJECT
 
     public:
-    bool running = false;
+    std::atomic<bool> running{false};
+    void requestStop() noexcept { running.store(false, std::memory_order_relaxed); }
     public slots:
     void startServer();
     void stopServer();
     signals:
     void sendData1(QString);
+    void serverStopped();
 };
 
 #endif // SERVER_MAIN_CPP
diff --git a/core/src/server_main.cpp b/core/src/server_main.cpp
index 72046c1..02c021d 100644
--- a/core/src/server_main.cpp
+++ b/core/src/server_main.cpp
@@ -7,10 +7,11 @@
 #include "../include/discovery_service.h"
 #include "../include/server_main.h"
 
-#include "QThread"
+#include <QThread>
+#include <QCoreApplication>
 
 void Server::startServer() {
-    running = true;
+    running.store(true, std::memory_order_relaxed);
     VirtualGamepad gp;
     if (!gp.init()) {
         emit sendData1("Gamepad init failed");
@@ -47,17 +48,23 @@ void Server::startServer() {
 
     emit sendData1("GAMEPAD INITIALIZATION SUCCESS");
 
-    while (running) {
+    while (running.load(std::memory_order_relaxed)) {
         int n = udp.receive(&pkt, sizeof(pkt), &sender);
         if (n > 0) {
             engine.processPacket(&pkt, n, sender);
         }
+        else{
+            QThread::msleep(1);
+        }
+        QCoreApplication::processEvents();
     }
+    udp.stop();
     discovery.stop();
     emit sendData1("Server Stopped");
+    emit serverStopped();
     return ;
 }
 
 void Server::stopServer(){
-    running = false;
+    requestStop();
 }
\ No newline at end of file
diff --git a/gui/mainwindow.cpp b/gui/mainwindow.cpp
index b35ce7e..b0ce29e 100644
--- a/gui/mainwindow.cpp
+++ b/gui/mainwindow.cpp
@@ -16,7 +16,9 @@ MainWindow::MainWindow(QWidget *parent)
 MainWindow::~MainWindow()
 {
     if (thread) {
-        server->stopServer();
+        if (server) {
+            server->requestStop();
+        }
         thread->quit();
         thread->wait();
     }
@@ -45,8 +47,14 @@ void MainWindow::on_systemStart_clicked()
         ui->logs_text->appendPlainText(msg);
     });
 
+    connect(server, &Server::serverStopped, thread, &QThread::quit);
+
     connect(thread, &QThread::finished, server, &QObject::deleteLater);
     connect(thread, &QThread::finished, thread, &QObject::deleteLater);
+    connect(thread, &QThread::finished, this, [this]() {
+        server = nullptr;
+        thread = nullptr;
+    });
 
     thread->start();
 }
@@ -55,13 +63,8 @@ void MainWindow::on_systemStop_clicked()
 {
     if(!server || !thread) return ;
 
-    server->stopServer();
-
+    server->requestStop();
     thread->quit();
-    thread->wait();
-
-    server = nullptr;
-    thread = nullptr;
 
 }
 
